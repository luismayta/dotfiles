# shellcheck shell=bash
# macOS-specific herdr configuration
export HERDR_CLIPBOARD_COPY_CMD="${HERDR_CLIPBOARD_COPY_CMD:-pbcopy}"
export HERDR_CLIPBOARD_PASTE_CMD="${HERDR_CLIPBOARD_PASTE_CMD:-pbpaste}"
