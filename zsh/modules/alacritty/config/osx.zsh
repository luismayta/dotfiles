# shellcheck shell=bash
# macOS-specific alacritty config (no-op — alacritty cask deprecated on macOS, use wezterm)
# shellcheck disable=SC2034 # consumed by plugin.zsh via config/main.zsh
ZSH_ALACRITTY_ENABLED=false
export ALACRITTY_APPLICATION="/Applications/Alacritty.app"
