# shellcheck shell=bash

function issues::task::create {
    message_warning "No provider detected. Please run from a git repository."
}

function issues::pr {
    message_warning "No provider detected. Please run from a git repository."
}
