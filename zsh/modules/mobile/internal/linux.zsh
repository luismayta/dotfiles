# shellcheck shell=bash
# Linux-specific mobile internal functions
#
# Android SDK installation on Linux is handled by the per-platform
# logic in mobile::internal::android::install (internal/base.zsh)
# which checks OSTYPE and uses direct Google download for cmdline-tools.
