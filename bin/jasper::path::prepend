#!/usr/bin/env zsh

# Source core framework to make core::path::prepend available
DOTFILES_DIR="${DOTFILES_DIR:-${HOME}/.dotfiles}"
source "${DOTFILES_DIR}/zsh/core/main.zsh"

core::path::prepend "${1}"
