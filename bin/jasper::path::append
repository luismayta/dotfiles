#!/usr/bin/env zsh

# Source core framework to make core::path::append available
DOTFILES_PATH="${DOTFILES_PATH:-${HOME}/.dotfiles}"
DOTFILES_CORE_PATH="${DOTFILES_PATH}/zsh/system/core"
source "${DOTFILES_CORE_PATH}/main.zsh"

core::path::append "${1}"
