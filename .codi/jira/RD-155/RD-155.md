# RD-155: Agregar omp como herramienta del módulo AI siguiendo arquitectura de tres capas

## Contenido Fuente

### Scenario

omp (Oh My Pi) es un coding agent con IDE integrado, fork de Pi by Mario Zechner.
Instalable via `curl -fsSL https://omp.sh/install | sh` o `bun install -g @oh-my-pi/pi-coding-agent`.
Config en `~/.omp/agent/`. Requiere bun >= 1.3.14.

Se debe implementar en `zsh/modules/ai/` siguiendo la guía `docs/guides/implement-tool-in-module.md` y el patrón de tres capas usado por opencode, pi, y otras herramientas del módulo.

### Acceptance Tests

- `config/omp.zsh` creado con variables `ZSH_AI_OMP_*` (BIN_PATH, CONFIG_PATH, CONFIG_SOURCE_PATH, INSTALL_URL)
- `internal/omp.zsh` creado con funciones `ai::internal::omp::load`, `ai::internal::omp::install`, `ai::internal::omp::config::sync`
- `pkg/omp.zsh` creado con funciones públicas `ai::omp::install`, `ai::omp::config::sync`
- `omp` agregado al array `ZSH_AI_TOOLS` en `config/base.zsh`
- `source` de config/omp.zsh agregado en `config/base.zsh`
- `source` de internal/omp.zsh agregado en `internal/main.zsh`
- `source` de pkg/omp.zsh agregado en `pkg/main.zsh`
- `ai::internal::omp::load` invocado en `internal/main.zsh`
- Guard `core::exists omp` presente en load
- Instalación via curl si no existe (patrón similar a pi)
- Module carga sin errores: `source zsh/system/core/main.zsh && source zsh/modules/ai/plugin.zsh`
- Función verificable: `type ai::omp::install` retorna "function"

### Sources

- https://omp.sh/
- https://omp.sh/docs/quickstart
- https://github.com/can1357/oh-my-pi
- docs/guides/implement-tool-in-module.md
- https://github.com/luismayta/dotfiles.git

---

## Enriquecimiento

Status: skipped_no_context_queries

---

### CodeGraph Enrichment
Status: partial

#### codegraph_explore: `omp pi opencode ZSH_AI_TOOLS ai::omp::install ai::internal::omp::load`

El índice CodeGraph existe pero NO cubre archivos zsh (solo devolvió símbolos lua de `zsh/modules/hammerspoon/`, irrelevantes para esta tarea). El contexto estructural real se obtuvo vía descubrimiento de filesystem:

- `zsh/modules/ai/config/pi.zsh` — patrón de variables: `ZSH_AI_PI_BIN_PATH`, `ZSH_AI_PI_CONFIG_PATH`, `ZSH_AI_PI_CONFIG_SOURCE_PATH`, `ZSH_AI_INSTALL_URL_PI`
- `zsh/modules/ai/internal/pi.zsh` — `ai::internal::pi::load` (PATH si binario existe), `ai::internal::pi::install` (guard `core::exists pi` + `curl -fsSL ... | sh`), `ai::internal::pi::config::sync` (rsync de `data/pi/` → `$HOME/.pi/agent/`)
- `zsh/modules/ai/pkg/pi.zsh` — fachada pública delgada: `ai::pi::install`, `ai::pi::config::sync` delegando a internal
- `zsh/modules/ai/config/base.zsh` — array `ZSH_AI_TOOLS` (15 tools) + sources de config/*.zsh en orden de dependencia
- `zsh/modules/ai/internal/main.zsh` — sources de internal/*.zsh + bloque "Load paths" donde se invoca `ai::internal::pi::load`
- `zsh/modules/ai/pkg/main.zsh` — sources de pkg/*.zsh
- `zsh/modules/ai/plugin.zsh` — cadena config/main.zsh → internal/main.zsh → pkg/main.zsh + guards de auto-install (rsync, fzf, bunx)
- `zsh/system/core/pkg/base.zsh` — define `core::exists` (delega a `core::internal::core::exists` en `zsh/system/core/internal/api.zsh`)
- `docs/guides/implement-tool-in-module.md` — guía de tres capas (config/internal/pkg) con prefijo de referencia `DEVOPS_<TOOL>_` / `devops::<tool>::`; el módulo AI real usa `ZSH_AI_<TOOL>_` / `ai::<tool>::`

#### codegraph_search: `ZSH_AI_TOOLS`

No results found — los archivos zsh no están indexados en CodeGraph.

---

## Instrucciones

Genera una especificación OpenSpec en markdown, en inglés, con trazabilidad a RD-155.

Rules:
- Usa SOLO la información proporcionada — NO inventes información
- Convierte acceptance tests en requerimientos usando MUST / SHOULD / MAY
- Incluye file paths del enrichment como contexto de código relevante