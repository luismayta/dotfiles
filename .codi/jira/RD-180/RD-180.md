# RD-180: Registrar plugin herdr-espresso en módulo herdr de dotfiles para mantener Mac despierta con agente IA

## Contenido Fuente

### Scenario

El módulo herdr de dotfiles (zsh/modules/herdr/) gestiona plugins de forma declarativa a través del array ZSH_HERDR_INSTALL_PLUGINS en config/plugins.zsh, que se instalan automáticamente al cargar el módulo.

herdr-espresso es un plugin que mantiene la Mac despierta mientras el agente de codificación de un panel está trabajando activamente, y permite que la Mac entre en suspensión cuando el agente se vuelve inactivo. Requisitos: macOS, herdr 0.7.0+ y CLI espresso en PATH.

### Acceptance Tests


- Registrar `Hanyang-Li/herdr-espresso` en el array `ZSH_HERDR_INSTALL_PLUGINS` de `zsh/modules/herdr/config/plugins.zsh`

- Verificar que al recargar el módulo zsh el plugin se instala automáticamente (`herdr plugin list` incluye herdr-espresso)

- Verificar que el CLI espresso está instalado y disponible en PATH

- (Opcional) Configurar keybinding `espresso.toggle` en `data/config.toml` para activar/desactivar el monitoreo


### Sources


- https://github.com/Hanyang-Li/herdr-espresso

- https://github.com/luismayta/dotfiles.git




---

## Enriquecimiento



Status: skipped_no_context_queries







### CodeGraph Enrichment
Status: partial


#### codegraph_explore: `ZSH_HERDR_INSTALL_PLUGINS herdr plugin install plugins.zsh`

```
El índice codegraph devolvió 18 símbolos en 5 archivos, pero ninguno del módulo herdr (zsh/modules/herdr/) — los resultados corresponden a hammerspoon (installSpoons, SpoonInstall) y .opencode/plugins/graphify.js. El módulo herdr no está cubierto por el índice. Contexto estructural del módulo herdr obtenido por exploración directa del codebase:
- Registro declarativo de plugins: zsh/modules/herdr/config/plugins.zsh — array ZSH_HERDR_INSTALL_PLUGINS con ~17 plugins en 5 categorías (formato owner/repo)
- Instalación automática: zsh/modules/herdr/internal/install.zsh (herdr::internal::plugin::install::all) — itera el array y ejecuta `herdr plugin install <owner/repo> --yes` al cargar plugin.zsh
- Keybindings de plugins: zsh/modules/herdr/data/config.toml — sección [[keys.command]] con type = "plugin_action"
- Datos bundled de plugins: zsh/modules/herdr/data/plugins/config/ (ej. cloudmanic.herdr-plus)
- No existe referencia a 'espresso' en el módulo actualmente
```





---

## Instrucciones

Genera una especificación OpenSpec en markdown, en inglés, con trazabilidad a RD-180.

Rules:
- Usa SOLO la información proporcionada — NO inventes información
- Convierte acceptance tests en requerimientos usando MUST / SHOULD / MAY
- Incluye file paths del enrichment como contexto de código relevante
