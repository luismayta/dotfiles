# RD-174: Crear script de instalacion y configuracion de chrome-headless-shell y mmdc para renderizado de diagramas mermaid

## Contenido Fuente

### Scenario

El renderizado de diagramas mermaid con mmdc (mermaid-cli) requiere chrome-headless-shell. La instalacion actual se realizo via npx @puppeteer/browsers y vive en /tmp (efimera). Se necesita un setup reproducible y persistente en dotfiles para que el renderizado de diagramas funcione de forma estable. La implementacion vive en el modulo zsh/modules/nodejs/, que ya gestiona toolchains de Node.js (fnm, bun, paquetes npm globales) con instalacion persistente y auto-install guards. Directiva: mmdc (@mermaid-js/mermaid-cli) debe agregarse al array NODEJS_PACKAGES en zsh/modules/nodejs/config/base.zsh para instalarse via el mecanismo existente `bun install -g` (nodejs::internal::packages::install); chrome-headless-shell requiere instalacion persistente propia en ~/.local/share/chrome-headless-shell via npx @puppeteer/browsers, mas puppeteer-config.json con executablePath.

### Acceptance Tests


- Script de instalacion de chrome-headless-shell en ubicacion persistente (~/.local/share/chrome-headless-shell)

- Configuracion de puppeteer-config.json con executablePath correcto

- mmdc renderiza diagramas mermaid sin errores

- Documentacion del setup en dotfiles


### Sources


- npx @puppeteer/browsers install chrome-headless-shell@stable

- https://github.com/luismayta/dotfiles.git




---

## Enriquecimiento



Status: skipped_no_context_queries







### CodeGraph Enrichment
Status: applied


#### glob: `zsh/modules/nodejs/**`

```
Module structure:
- zsh/modules/nodejs/Taskfile.yml (readme + sync:npmrc tasks)
- zsh/modules/nodejs/plugin.zsh (entry point: config/main.zsh -> internal/main.zsh -> pkg/main.zsh)
- zsh/modules/nodejs/config/base.zsh (env vars: FNM_PATH=~/.local/share/fnm, BUN_INSTALL=~/.bun, NODEJS_PACKAGES, NODEJS_DATA_PATH, NODEJS_NPMRC_PATH)
- zsh/modules/nodejs/internal/base.zsh (install engine: fnm::install, bun::install, packages::install, version::all::install, sync)
- zsh/modules/nodejs/internal/main.zsh (auto-install guards: core::ensure curl/unzip/rsync; if ! core::exists fnm; then install; fi)
- zsh/modules/nodejs/pkg/base.zsh (public API: nodejs::install, nodejs::load, nodejs::sync, nodejs::package::all::install)
- zsh/modules/nodejs/data/sync/.npmrc (template synced to ~/ via rsync)
```




#### codegraph_explore: `nodejs::internal::fnm::install nodejs::internal::sync nodejs::internal::packages::install`

```
Install patterns in zsh/modules/nodejs/internal/base.zsh:
- nodejs::internal::fnm::install: curl -fsSL "${FNM_INSTALL_URL}" | bash; then fnm::load (exports PATH + eval fnm env)
- nodejs::internal::fnm::load: if [ -d "${FNM_PATH}" ]; then export PATH="${FNM_PATH}:${PATH}"; eval "$(fnm env)"; fi
- nodejs::internal::packages::install: bun install -g "${NODEJS_PACKAGES[@]}"
- nodejs::internal::bun::install: if core::exists bun; then return 0; fi; curl -fsSL "${BUN_INSTALL_URL}" | bash
- nodejs::internal::sync: rsync -avzh --progress "${NODEJS_DATA_PATH}/sync/" "${HOME}/"
Auto-install guard (internal/main.zsh): if ! core::exists fnm; then nodejs::internal::fnm::install; fi
```




#### codegraph_node: `zsh/modules/nodejs/config/base.zsh NODEJS_PACKAGES`

```
NODEJS_PACKAGES array (lines 13-40) in zsh/modules/nodejs/config/base.zsh is installed via `bun install -g "${NODEJS_PACKAGES[@]}"` (nodejs::internal::packages::install in internal/base.zsh). DIRECTIVE: add @mermaid-js/mermaid-cli (mmdc) to this array so it installs via the existing bun global mechanism — no separate installer needed. chrome-headless-shell still requires its own persistent install (~/.local/share/chrome-headless-shell via npx @puppeteer/browsers) + puppeteer-config.json with executablePath.
```




#### grep: `persistent install patterns (~/.local/share, ~/.local/bin)`

```
Persistent install destinations in dotfiles:
- ~/.local/bin: smolvm (zsh/modules/smolvm/internal/install.zsh: curl tarball -> verify SHA256 -> mkdir -p -> extract -> cp to ~/.local/bin -> core::path::prepend), AI tools (shimmy, rtk, graphify, codegraph)
- ~/.local/share/fnm: nodejs fnm (curl|bash official installer)
- ~/.bun: nodejs bun (curl|bash official installer)
- ~/.local/share/nvim, ~/.local/share/helix: data dirs
Taskfile convention: only readme task (gomplate README.yaml -> README.md); nodejs adds sync:npmrc. Install logic lives in zsh internal/main.zsh auto-guards, NOT in Taskfiles.
```




#### grep: `mermaid mmdc puppeteer chrome-headless`

```
No existing mermaid/mmdc/puppeteer/chrome-headless files in dotfiles (greenfield). Only AI prompt patterns exist: zsh/modules/ai/data/patterns/create_mermaid_visualization/system.md and create_mermaid_visualization_for_github/system.md (mermaid syntax generation, not tooling).
```





---

## Instrucciones

Genera una especificación OpenSpec en markdown, en inglés, con trazabilidad a RD-174.

Rules:
- Usa SOLO la información proporcionada — NO inventes información
- Convierte acceptance tests en requerimientos usando MUST / SHOULD / MAY
- Incluye file paths del enrichment como contexto de código relevante
