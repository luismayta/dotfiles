# HAD-100: Reemplazar pyenv por uv como gestor de versiones de Python en zsh/modules/python con 3.14 como versión estándar

## Contenido Fuente

### Scenario

Reemplazar pyenv por uv como gestor de versiones de Python en el módulo `zsh/modules/python`, estableciendo Python 3.14 como versión estándar global.

### Acceptance Tests

_No acceptance tests defined in the Jira issue (description is null). Requirements MUST be derived from the Scenario above._

### Sources

_None provided in the Jira issue._

---

## Enriquecimiento

Status: skipped_no_context_queries

Reason: La descripción del issue es null y no contiene consultas `mcp obsidian:`; se omite el enriquecimiento vía Obsidian.

### CodeGraph Enrichment
Status: applied

#### codegraph_explore: `pyenv uv python version manager zsh/modules/python module`

```
- nix/versions.nix defines central dev-shell versions: pythonVersion = "3.11"
  (pkgs.python311). Header mandates syncing Taskfile.yml vars when bumping
  (PYTHON_VERSION -> pythonVersion).
- Taskfile.yml:166 defines PYTHON_VERSION: 3.11.5 — second source of truth;
  both MUST move to 3.14.x in sync.
- SCOPE DIRECTIVE (Luchex): NO file under openspec/ is modified by HAD-100 —
  archived changes AND live specs are read-only context. Known behavioral
  drift after removing pyenv (documented, not fixed here):
  - openspec/specs/python-module/spec.md — still mandates pyenv toggles
    (PYTHON_PYENV_ENABLED), install/load at $PYTHON_ROOT (~/.pyenv), versions
    via $PYTHON_VERSIONS / $PYTHON_VERSION_GLOBAL, and a status command
    reporting pyenv root + uv status + toggles.
  - openspec/specs/core-messages/spec.md — SHALL-NOT list includes
    CORE_MESSAGE_PYENV.
  - openspec/specs/cleanup-safety/spec.md — scenario "pyenv not removed by
    cleanup::all": system SHALL NOT delete ~/.pyenv/versions.
  Note: zsh/system/core/config/env.zsh:12 (CORE_MESSAGE_PYTHON message text)
  IS runtime code and remains IN SCOPE.
- Prior migration (openspec/changes/archive/2026-06-28-migrate-pyenv-to-python-module):
  uv is ALREADY first-class inside zsh/modules/python via PYTHON_UV_ENABLED
  toggle; HAD-100 replaces only the pyenv half, keeping uv.
- Target module files (zsh/modules/python/): plugin.zsh,
  internal/{base,main}.zsh, pkg/{base,main}.zsh, config/{base,main,osx,linux}.zsh
  - internal/base.zsh — python::internal::pyenv::install (git clone),
    ::load (lazy load; pyenv install/global via PYTHON_VERSION_GLOBAL)
  - config/base.zsh — PYTHON_PYENV_ENABLED=true, PYTHON_ROOT=${HOME}/.pyenv,
    PYTHON_PACKAGE_NAME=pyenv, PYTHON_INSTALL_URL=github.com/pyenv/pyenv.git
- Adjacent runtime references: zsh/zshenv (PYENV_ROOT/PATH wiring,
  PIPENV_PYTHON=${PYENV_ROOT}/shims/python, PYENV_VIRTUALENV_DISABLE_PROMPT),
  zsh/modules/clean/pkg/base.zsh (cleanup::python::pyenv, informative only),
  zsh/modules/starship/data/starship.toml:222 (pyenv_version_name = true —
  starship API key; becomes dead config once pyenv is removed).
- Archive-only mentions (historical, no action required): CHANGELOG.md and
  openspec/changes/archive/* (migrate-zsh-pyenv, standardize-install-urls,
  standardize-message-vars, harden-clean-safety, refactor-starship-toml).
```

---

## Instrucciones

Genera una especificación OpenSpec en markdown, en inglés, con trazabilidad a HAD-100.

Rules:
- Usa SOLO la información proporcionada — NO inventes información
- Convierte acceptance tests en requerimientos usando MUST / SHOULD / MAY
- Incluye file paths del enrichment como contexto de código relevante
