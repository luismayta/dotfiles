# RD-125: Fix fragile UUID parsing, config update, deprecated url directive, DNS idempotency, and service validation

## Contenido Fuente

### Scenario

The cloudflared tunnel create function in zsh/modules/devops has several reliability issues that can cause silent failures or incorrect configurations:

1. UUID lookup uses fragile tail/awk parsing of `cloudflared tunnel list` output instead of --format json
2. Config file (config.yml) is never updated on re-run, even if hostname or port changes
3. No-hostname config uses deprecated `url:` directive instead of modern `ingress:` format
4. DNS routing does not check if already routed before attempting
5. No validation that the target port is actually listening before tunnel creation

### Acceptance Tests

- [ ] UUID lookup uses `cloudflared tunnel list --format json` instead of fragile tail/awk parsing
- [ ] Config file is updated when hostname or port changes on re-run (not just scaffolded if missing)
- [ ] No-hostname config uses `ingress:` format instead of deprecated `url:` directive
- [ ] DNS routing checks if already routed before attempting `cloudflared tunnel route dns`
- [ ] Port availability is verified (e.g. via nc or ss) before tunnel creation
- [ ] All changes maintain backward compatibility with existing tunnel configurations

### Sources

- https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/get-started/create-local-tunnel/
- https://github.com/luismayta/dotfiles.git

---

## Enriquecimiento

Status: applied

### Contexto derivado de: cloudflared tunnel zsh devops

No Obsidian notes found for this topic (vault search returned empty).

---

### CodeGraph Enrichment
Status: applied

#### codegraph_explore: `cloudflared tunnel create zsh devops`

**Primary target file:** `zsh/modules/devops/internal/cloudflared.zsh` (137 lines)

Function `devops::cloudflared::internal::tunnel::create` (lines 59–137):
- Accepts `name`, `port`, `hostname` (optional)
- Validates port is numeric
- Checks `cloudflared` binary exists
- Checks `cert.pem` exists for auth
- **Line 85 (UUID lookup):** Uses fragile `cloudflared tunnel list 2>/dev/null | tail -n +2 | awk -v n="${name}" '$2 == n {print $1; exit}'`
- **Line 93:** Repeats same fragile parsing after creating tunnel
- **Lines 111–132 (Config scaffold):** Only writes config.yml if file does NOT exist (`[[ ! -f "${config_file}" ]]`)
- **Lines 126–130:** No-hostname path uses deprecated `url:` directive:
  ```
  url: http://localhost:${port}
  tunnel: ${uuid}
  credentials-file: ${DEVOPS_CLOUDFLARED_CONFIG_DIR}/${uuid}.json
  ```
- **Lines 103–109 (DNS routing):** Calls `cloudflared tunnel route dns` without checking if already routed
- **No port validation** before tunnel creation

**Public API wrapper:** `zsh/modules/devops/pkg/cloudflared.zsh` (18 lines)
- `devops::cloudflared::tunnel::create` delegates to `devops::cloudflared::internal::tunnel::create`

**Config:** `zsh/modules/devops/config/cloudflared.zsh` (13 lines)
- Exports: `DEVOPS_CLOUDFLARED_PACKAGE_NAME`, `DEVOPS_CLOUDFLARED_VERSION`, `DEVOPS_CLOUDFLARED_DOWNLOAD_URL`, `DEVOPS_CLOUDFLARED_SHA256`, `DEVOPS_CLOUDFLARED_ROOT_BIN`, `DEVOPS_CLOUDFLARED_BIN`, `DEVOPS_CLOUDFLARED_CONFIG_DIR`

---

## Instrucciones

Genera una especificación OpenSpec en markdown, en inglés, con trazabilidad a RD-125.

Rules:
- Usa SOLO la información proporcionada — NO inventes información
- Convierte acceptance tests en requerimientos usando MUST / SHOULD / MAY
- Incluye file paths del enrichment como contexto de código relevante
