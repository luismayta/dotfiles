# RD-107: Implementar la tool docker-compose en zsh/modules/devops/ siguiendo la guía implement-tool-in-module.md

## Contenido Fuente

### Scenario

Docker Compose es una tool standalone de la familia DevOps que debe integrarse al módulo `devops` siguiendo la guía `docs/guides/implement-tool-in-module.md`.

La integración sigue la arquitectura de tres capas del módulo:

* `config/` → variables de entorno con prefijo `DEVOPS_DOCKER_COMPOSE_`
* `internal/` → funciones privadas `devops::docker-compose::internal::*` (load, install, upgrade, main::factory)
* `pkg/` → API pública `devops::docker-compose::*` (install, upgrade)

Docker Compose es un CLI standalone sin shell hooks → patrón PATH-only (referencia: bruno). El load solo verifica existencia con `core::exists` y el acceso se da vía PATH. No requiere `eval` ni keybindings.

Debe registrarse la tool en el array `DEVOPS_TOOLS` de `config/base.zsh` y respetar las convenciones de la guía (prefijos, guards, mensajes).

### Acceptance Tests

- Crear `zsh/modules/devops/config/docker-compose.zsh` con variables `DEVOPS_DOCKER_COMPOSE_*` (PACKAGE_NAME, INSTALL_CMD, CONFIG_DIR)
- Crear `zsh/modules/devops/internal/docker-compose.zsh` con `devops::docker-compose::internal::load`, `install`, `upgrade` y `main::factory`
- Crear `zsh/modules/devops/pkg/docker-compose.zsh` con funciones públicas `devops::docker-compose::install` y `upgrade`
- Registrar `docker-compose` en el array `DEVOPS_TOOLS` de `zsh/modules/devops/config/base.zsh`
- Aplicar patrón PATH-only: guard `core::exists docker-compose` en load; sin `eval` en config
- Usar `message_info`/`message_success`/`message_error` para feedback (prohibido `echo`)
- No hardcodear comandos de instalación — usar variables `DEVOPS_DOCKER_COMPOSE_INSTALL_*`
- El módulo carga sin errores: `source zsh/system/core/main.zsh && source zsh/modules/devops/plugin.zsh`
- Verificar funciones disponibles: `type devops::docker-compose::install` → `function`
- Todas las funciones usan prefijo `devops::docker-compose::` (nunca single underscore)

### Sources

- `docs/guides/implement-tool-in-module.md` — guía de implementación (checklist y anti-patterns)
- `zsh/modules/devops/config/bruno.zsh`, `internal/bruno.zsh`, `pkg/bruno.zsh` — referencia patrón PATH-only
- `zsh/modules/devops/config/atuin.zsh`, `internal/atuin.zsh`, `pkg/atuin.zsh` — referencia patrón shell hooks
- `zsh/modules/devops/config/base.zsh` — registro del array `DEVOPS_TOOLS`
- https://github.com/luismayta/dotfiles.git

---

## Enriquecimiento

Status: skipped_no_context_queries

---

## CodeGraph Enrichment

Status: partial

#### codegraph_explore: `devops docker-compose DEVOPS_TOOLS install upgrade`
El índice CodeGraph está inicializado pero NO cubre los símbolos del módulo zsh `devops`. La búsqueda devolvió 56 símbolos en 26 archivos, todos del área hammerspoon (Lua): `installSpoons`, `updateRepo`, `asyncInstallSpoonFromZipURL` (zsh/modules/hammerspoon/...). Ningún símbolo `devops::*` ni `DEVOPS_TOOLS` fue indexado.

#### codegraph_explore: `DEVOPS_TOOLS devops::bruno internal load factory plugin.zsh`
Devolvió 16 símbolos en 10 archivos, todos de hammerspoon/Lua (`loader.lua`, `config/init.lua`, `speed.lua`, Caffeine.spoon, HeadphoneAutoPause.spoon). Cero cobertura del módulo `zsh/modules/devops/`.

#### codegraph_search: `devops`
1 resultado no relacionado: `devs` (variable) en zsh/modules/hammerspoon/data/Spoons/HeadphoneAutoPause.spoon/init.lua:90.

#### codegraph_search: `DEVOPS_TOOLS`
No results found.

Nota: los archivos fuente del módulo existen en disco (zsh/modules/devops/{config,internal,pkg}/), pero no están indexados. Los ejecutores deben leer directamente `docs/guides/implement-tool-in-module.md` y las referencias bruno/atuin.

---

## Instrucciones

Genera una especificación OpenSpec en markdown, en inglés, con trazabilidad a RD-107.

Rules:
- Usa SOLO la información proporcionada — NO inventes información
- Convierte acceptance tests en requerimientos usando MUST / SHOULD / MAY
- Incluye file paths del enrichment como contexto de código relevante
