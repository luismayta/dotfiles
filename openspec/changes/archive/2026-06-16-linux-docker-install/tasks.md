## 1. Linux Docker Install

- [x] 1.1 Add Linux-specific install path in `internal/docker.zsh` with OSTYPE check: install via `sudo paru -S`, enable systemd service, add user to docker group, verify with hello-world
