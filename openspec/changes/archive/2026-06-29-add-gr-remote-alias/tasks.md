## 1. Add `gr` alias

- [x] 1.1 Add `alias gr='git remote'` line in `zsh/modules/git/pkg/alias.zsh` (between `gm` and `grm`, maintaining alphabetical grouping)

## 2. Verification

- [x] 2.1 Alias `gr='git remote'` verificado en `pkg/alias.zsh` — correcto
- [x] 2.2 Sin conflictos: no existía `gr` previo, `grm` es `git rm`, nombres distintos
