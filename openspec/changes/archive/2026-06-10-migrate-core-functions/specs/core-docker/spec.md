## REMOVED Requirements

### Requirement: Docker-based AWS CLI

**Reason**: Migrated to new `zsh/modules/docker/` module.
**Migration**: `awscli()` relocated to `zsh/modules/docker/pkg/helper.zsh`. Same signature and behavior.

### Requirement: Docker-based AWS Shell

**Reason**: Migrated to new `zsh/modules/docker/` module.
**Migration**: `aws-shell()` relocated to `zsh/modules/docker/pkg/helper.zsh`. Same signature and behavior.

### Requirement: Docker-based YouTube-DL

**Reason**: Migrated to new `zsh/modules/docker/` module.
**Migration**: `ytdl()`, `ytd-mp3()`, `youtube-dl()` relocated to `zsh/modules/docker/pkg/helper.zsh`. Same signatures and behavior.

### Requirement: Docker-based nyan cat

**Reason**: Migrated to new `zsh/modules/docker/` module.
**Migration**: `nyancat()` relocated to `zsh/modules/docker/pkg/helper.zsh`. Same signature and behavior.

### Requirement: Docker-based Komiser dashboard

**Reason**: Migrated to new `zsh/modules/docker/` module.
**Migration**: `komiser()` relocated to `zsh/modules/docker/pkg/helper.zsh`. Same signature and behavior.
